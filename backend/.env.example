# Backend Environment Variables
# Copy this file to .env in the backend directory

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option 1: Neon Serverless PostgreSQL (Recommended for Production)
# Get your connection string from https://neon.tech dashboard
DATABASE_URL="postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/dbname?sslmode=require"

# Option 2: Local PostgreSQL (Development)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/taskflow"

# Option 3: SQLite (Quick Testing Only - Not for Production)
# DATABASE_URL="sqlite:///./todo_app.db"

# ============================================
# AUTHENTICATION CONFIGURATION
# ============================================

# JWT signing secret - MUST be at least 32 characters
# Generate a strong secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: This MUST match the BETTER_AUTH_SECRET in frontend/.env.local
BETTER_AUTH_SECRET="your-super-secret-jwt-signing-key-change-in-production-min-32-chars"

# ============================================
# API CONFIGURATION
# ============================================

# Host address (0.0.0.0 allows external connections)
API_HOST="0.0.0.0"

# Port number for the API server
API_PORT=8000

# CORS allowed origins (comma-separated for multiple origins)
# For development: http://localhost:3000
# For production: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS="http://localhost:3000"

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Debug mode (set to False in production)
# DEBUG=False

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO